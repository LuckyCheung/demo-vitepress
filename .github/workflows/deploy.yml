name: deploy-action

on:
  push:
    branches:
      - master
    # tags: # 当我们提交代码为tag 是以'v'开头的时候才会触发自动部署到服务端 如 git push tag v0.1.0
    #   - "v*"

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Branch
        uses: actions/checkout@v4

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm i

      - name: Build
        run: |
          npm run docs:build

      - name: Deploy Github
        uses: JamesIves/github-pages-deploy-action@v4.6.1
        with:
          token: ${{ secrets.password }}
          folder: docs/.vitepress/dist

      # - name: SyncFiles
      #   uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      #   with:
      #     target-server: www.sjhp.club
      #     remote-user: root
      #     private-ssh-key: ${{ secrets.SSH }}
      #     source-path: docs/.vitepress/dist
      #     destination-path: /docker/nginx-proxy-manager/www/www.sjhp.club/
